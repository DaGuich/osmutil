package geotypes

// Points2Polygon Creates a Polygon from Points
func Points2Polygon(points []*Point) *Polygon {
	var (
		sortedCoords []*Coord
		// centerPoint  *Coord
	)

	return NewPolygon(sortedCoords)
}
